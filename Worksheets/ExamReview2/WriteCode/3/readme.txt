Exam Review 2 - Question WC-3: Complexity

Write a class to store a Complex Number (an integer real part and an integer imaginary part) that
implements the following functions:
    Complex(int,int);
    ~Complex();
    operator+
    operator-
    operator+=
    operator-=
    operator<<